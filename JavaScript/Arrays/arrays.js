const array = [1, "strings", true]; //Como se arma un array

const position = 1; //asignacion valor

console.log(array[position]); //Acceso al valor

array[position] = "Hola"; //Resigno el valor de la posicion

console.log(array[position]);

const numbers = [1, 2, 3, 4, 5];
numbers[0] = 10;
numbers[2] = 30;
console.log(numbers);
