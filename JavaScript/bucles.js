//Bucles

const arrays = [1,2,3];
let contador = 0;

while(contador < 2) {
    console.log('Hola');
    contador++;
    console.log(contador);
}

for(let i = 0; i < arrays.length; i++) {
    console.log("array", arrays[1]);
}